#Если Сервер Тогда

&Вместо("ПолучитьМакетИзменений")
Функция МОД_ПолучитьМакетИзменений(Форма)
	ПараметрыСеанса.МОД_ИмяПоследнейФормы = Форма.ИмяФормы;
	
	АдресМакетаИзменений = ПараметрыСеанса.МОД_АдресМакетаИзменений.Получить(ПараметрыСеанса.МОД_ИмяПоследнейФормы);
	Если АдресМакетаИзменений <> Неопределено Тогда
		Результат = ПолучитьИзВременногоХранилища(АдресМакетаИзменений);
	Иначе
		Результат = ПродолжитьВызов(Форма);
	КонецЕсли;
	
	Возврат Результат;
КонецФункции

#КонецЕсли